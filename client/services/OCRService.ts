/**
 * @deprecated 2026-06-25 — OCR 服务已删除
 *
 * 历史：v1.7.0 之前 OCRService.ts 提供 JS 侧 OCR 文字识别服务
 *       全套 0 调用方：grep OCRService/ocrService 全部 0 命中
 *       千机+保利走节点树定位，不走 OCR
 *
 * 还原方法：
 *   git log -- services/OCRService.ts
 *   git checkout <commit-hash> -- services/OCRService.ts
 */
